# Host the server binds to (required)
HOST=localhost
# Port the server listens on (required; use 3001 in dev so the
# web app can keep 3000)
PORT=3001

# Database URL for PostgreSQL
DATABASE_URL="postgresql://username:password@localhost:5432/mydb"

# Clerk keys (both required to enable authentication)
CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=

# Svix signing secret of the Clerk webhook endpoint (starts with whsec_);
# required for POST /webhooks/clerk to accept events
CLERK_WEBHOOK_SIGNING_SECRET=

# Origin of the web app allowed to call this API from the browser
WEB_ORIGIN=http://localhost:3000
